apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myfirsthelm.fullname" . }}-test-connection"
  labels:
{{ include "myfirsthelm.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "myfirsthelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
